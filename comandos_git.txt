git init . >> cria um repo git na pasta atual
git status >> mostra o status do trabalho
git commit -m >> faz commit do que estava para ser commitado
git add >> adiciona arquivo que estava para ser commitado
